This example runs under Linux aarch64

The following software packages must be installed:

libboost-all-dev >= 1.58
libpcap-dev >= 0.8


To complile and run:

1. Enter directory "Release"
2. make clean
3. make
4. chmod 777 runexample.sh
5. ./runexample.sh